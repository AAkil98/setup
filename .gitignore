# macOS
.DS_Store

# Editor noise
*.swp
*.swo

# Backups created by the install scripts
backups/
*.backup

# Local secrets (MCP tokens, etc.) — NEVER commit
.secrets.local

# Homebrew bundle lockfile (if generated)
Brewfile.lock.json
