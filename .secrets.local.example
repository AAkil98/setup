# Copy this file to ".secrets.local" (which is gitignored) and fill in tokens.
#
#   cp .secrets.local.example .secrets.local
#
# scripts/08-claude-code.sh sources this to configure MCP servers without ever
# putting secrets in the repo. Keep real tokens ONLY in .secrets.local.

# Sanity hosted MCP (https://mcp.sanity.io) — bearer token.
export SANITY_MCP_TOKEN=""
